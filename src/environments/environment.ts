// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false
};

export const firebaseConfig = {
  apiKey: "AIzaSyAEO1FQQCwXNT9_kK_b-UHSTvzNPhiFrsc",
  authDomain: "loginfireb-e7d2b.firebaseapp.com",
  databaseURL: "https://loginfireb-e7d2b.firebaseio.com",
  projectId: "loginfireb-e7d2b",
  storageBucket: "loginfireb-e7d2b.appspot.com",
  messagingSenderId: "707702717285",
  appId: "1:707702717285:web:076b60c4d1494df1653f8c",
  measurementId: "G-XZPXH8QTHN"
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
